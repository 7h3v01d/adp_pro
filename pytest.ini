[pytest]
markers =
    network: tests that hit real external network endpoints (deselect with -m "not network")
    gui: tests that exercise PyQt6 widgets via pytest-qt
    torrent: tests that exercise the libtorrent-based torrent engine (a real local swarm, not the internet)
qt_api = pyqt6
testpaths = tests
