# Core runtime dependencies (Downloads + REST/MCP always work with just these).
PyQt6>=6.5
requests>=2.31
urllib3>=2.0
fastapi>=0.115
uvicorn>=0.30
mcp>=1.2,<2.0

# Torrent support is optional and native (no wheels on some platforms).
# Install it with the app:  pip install -e ".[torrents]"
# or on its own:           pip install "libtorrent>=2.0"
# The app runs fine without it -- the Torrents tab explains how to enable it.
